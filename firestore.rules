rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access to all documents
    // WARNING: These rules are for development only.
    // In production, restrict write access to authenticated admins.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
